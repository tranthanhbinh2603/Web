<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        Add a new product
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://getbootstrap.com/docs/5.3/assets/css/docs.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .dropdown p {
            display: inline;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>
            Edit product "<%= productWithID.name %>"
        </h1>
        <form action="../../product/<%= productWithID._id %>?_method=PUT" method="post">
            <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1">Name</span>
                <input type="text" class="form-control" id="name" class="name" aria-label="Username"
                    aria-describedby="basic-addon1" name="name" autocomplete="off" value="<%= productWithID.name %>">
            </div>
            <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1">Price</span>
                <input type="text" class="form-control" class="price" id="price" aria-label="Username"
                    aria-describedby="basic-addon1" name="price" autocomplete="off" value="<%= productWithID.price %>">
            </div>
            <div class="dropdown">
                <p>Type of product: </p>
                <select name="category" class="type" id="type">
                    <% for (let category of categories) { if (productWithID.category===category) { %>
                        <option value="<%= category %>" selected>
                            <%= category %>
                        </option>
                        <% } else { %>
                            <option value="<%= category %>">
                                <%= category %>
                            </option>
                            <% }} %>
                </select>
            </div>
            <button class="btn btn-primary">Submit</button>
        </form>
        <br>
        <a href="./products">Back to all products</a>
    </div>


</body>

</html>